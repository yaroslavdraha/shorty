const urls = (model) => {
  return {
    findByCode,
    insert
  }

  async function findByCode(code) {
    // db.find({code})
  }

  async function insert(url) {
    // db.url({code})
  }

}

module.exports = urls
